[package]
name = "tlottie-android"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.21", default-features = false }
libloading = "0.8"
tlottie = { path = "../../.." }

[workspace]
